/*
	// Har 
	public int findNearestSmallerThan(int value) {
		Node n = find(value); 
		if (n == null || root == null) {
			return 0;
		}

		else {
			int[] sortert = sortedArray();
			for (int i = 1; i < sortert.length; i++) {
				int sjekk = sortert[i];
				if (sjekk == value) {
					return sortert[i-1];
				}
			}
		}
		return 0;
	}
	*/